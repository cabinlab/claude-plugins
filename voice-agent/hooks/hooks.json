{
  "description": "Voice-agent bash security hooks (Layer 2). Strips credentials, blocks dangerous patterns, warns on network/package/destructive commands.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/bash-security.sh",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
